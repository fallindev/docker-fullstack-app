language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t korea13135/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true korea13135/react-test-app npm run test

after_success:
  - docker build -t korea13135/docker-frontend ./frontend
  - docker build -t korea13135/docker-backend ./backend
  - docker build -t korea13135/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push korea13135/docker-frontend
  - docker push korea13135/docker-backend
  - docker push korea13135/docker-nginx
